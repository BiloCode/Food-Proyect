{
  "name": "food",
  "private": true,
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "workspaces":[
    "packages/*"
  ],
  "scripts": {
    "start:admin": "lerna exec --scope @food/admin -- yarn start",
    "build:admin": "lerna exec --scope @food/admin -- yarn build",
    "start:web": "lerna exec --scope @food/web -- yarn start",
    "build:web": "lerna exec --scope @food/web -- yarn build"
  }
}
