import styled from "styled-components";

import { BLACK_LIGHT, FONT } from "config/styles";

export const CustomLabel = styled.label`
  font-weight: 600;
  font-family: ${FONT};
  color: ${BLACK_LIGHT};
`;
