import { FC } from "react";
import * as S from "./styles";

const ClientsRowsContainer: FC = ({ children }) => {
  return <S.Container>{children}</S.Container>;
};

export default ClientsRowsContainer;
