import { FC } from "react";
import * as S from "./styles";

const ClientRowTableGrid: FC = ({ children }) => {
  return <S.MainContainer>{children}</S.MainContainer>;
};

export default ClientRowTableGrid;
