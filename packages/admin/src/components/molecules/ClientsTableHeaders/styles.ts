import { FONT, NORMAL_GRAY } from "config/styles";
import styled from "styled-components";

export const TitleTable = styled.span`
  margin: 0;
  font-family: ${FONT};
  text-transform: uppercase;
  color: ${NORMAL_GRAY};
  font-weight: 550;
  text-align: center;
  font-size: 0.7em;
`;
